---
interface Props {
  name: string;
  url: string;
  icon: string;
}

const { name, url, icon } = Astro.props;
---

<a href={url} class="link" target="_blank" rel="noopener noreferrer" title={name}>
  <div class="icon" set:html={icon}></div>
  {name}
</a>
